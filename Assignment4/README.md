# Awesome Project Build with TypeORM

Steps to run this project:

1. Run `npm i` command
2. Setup database settings inside `data-source.ts` file
2.9. Run rm sakila-warehouse.db if it has already been populated.
3. Run `npm start <command>` command
- with following: Init, Full-Load, Incremental, Validate
